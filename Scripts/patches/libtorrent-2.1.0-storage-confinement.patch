diff --git a/include/libtorrent/aux_/file.hpp b/include/libtorrent/aux_/file.hpp
index d543c425829b43909a14fc3e32b9ad15a4949a43..c41a666ad6fd0de4eb5729418fe84f2e8c1dab9e 100644
--- a/include/libtorrent/aux_/file.hpp
+++ b/include/libtorrent/aux_/file.hpp
@@ -83,6 +83,8 @@ namespace libtorrent::aux {
 	{
 		file_handle(): m_fd(invalid_handle) {}
 		file_handle(string_view name, std::int64_t size, open_mode_t mode);
+		file_handle(string_view root, string_view relative, std::int64_t size
+			, open_mode_t mode);
 		file_handle(file_handle const& rhs) = delete;
 		file_handle& operator=(file_handle const& rhs) = delete;
 
diff --git a/include/libtorrent/aux_/file_descriptor.hpp b/include/libtorrent/aux_/file_descriptor.hpp
index 9dee4744c9a8e82ba02660b4140514135e63038d..6492294fda54b3824e6e28269656b49930757a34 100644
--- a/include/libtorrent/aux_/file_descriptor.hpp
+++ b/include/libtorrent/aux_/file_descriptor.hpp
@@ -43,11 +43,19 @@ struct file_descriptor
 	}
 
 	file_descriptor(file_descriptor const&) = delete;
-	file_descriptor(file_descriptor&& rhs)
+	file_descriptor(file_descriptor&& rhs) noexcept
 		: m_fd(rhs.m_fd)
 	{
 		rhs.m_fd = -1;
 	}
+	file_descriptor& operator=(file_descriptor&& rhs) noexcept
+	{
+		if (this == &rhs) return *this;
+		if (m_fd >= 0) ::close(m_fd);
+		m_fd = rhs.m_fd;
+		rhs.m_fd = -1;
+		return *this;
+	}
 	int fd() const { return m_fd; }
 private:
 	int m_fd;
diff --git a/include/libtorrent/aux_/file_pool.hpp b/include/libtorrent/aux_/file_pool.hpp
index 8ac0767c9ee4988392ac0f60b5e6dc493a21c546..48e7ce38d746f757cb0e4c383c38b0dc153fb15c 100644
--- a/include/libtorrent/aux_/file_pool.hpp
+++ b/include/libtorrent/aux_/file_pool.hpp
@@ -25,7 +25,8 @@ namespace libtorrent::aux {
 #endif
 
 		file_pool_entry(file_id k
-			, string_view name
+			, string_view root
+			, string_view relative
 			, open_mode_t const m
 			, std::int64_t const size
 #if TORRENT_HAVE_MAP_VIEW_OF_FILE
@@ -33,7 +34,7 @@ namespace libtorrent::aux {
 #endif
 			)
 			: key(k)
-			, mapping(std::make_shared<file_handle>(name, size, m))
+			, mapping(std::make_shared<file_handle>(root, relative, size, m))
 			, mode(m)
 		{}
 
@@ -54,4 +55,3 @@ namespace libtorrent::aux {
 }
 
 #endif
-
diff --git a/include/libtorrent/aux_/file_view_pool.hpp b/include/libtorrent/aux_/file_view_pool.hpp
index 2be38239213df3c60ea6845e288d651c3b2dc052..87c712a80e21c2c9060c68aa0689d3e6e954448b 100644
--- a/include/libtorrent/aux_/file_view_pool.hpp
+++ b/include/libtorrent/aux_/file_view_pool.hpp
@@ -27,7 +27,8 @@ namespace libtorrent::aux {
 		using lock_type = std::unique_lock<std::mutex>;
 
 		file_view_entry(file_id k
-			, string_view name
+			, string_view root
+			, string_view relative
 			, open_mode_t const m
 			, std::int64_t const size
 #if TORRENT_HAVE_MAP_VIEW_OF_FILE
@@ -35,7 +36,7 @@ namespace libtorrent::aux {
 #endif
 			)
 			: key(k)
-			, mapping(std::make_shared<file_mapping>(file_handle(name, size, m), m, size
+			, mapping(std::make_shared<file_mapping>(file_handle(root, relative, size, m), m, size
 #if TORRENT_HAVE_MAP_VIEW_OF_FILE
 				, open_unmap_lock
 #endif
diff --git a/include/libtorrent/aux_/part_file.hpp b/include/libtorrent/aux_/part_file.hpp
index 2450d976eb915c1718c7e5a71041eae8be68fb3f..abecf3134d0990a70ff551e364a5a67788defaca 100644
--- a/include/libtorrent/aux_/part_file.hpp
+++ b/include/libtorrent/aux_/part_file.hpp
@@ -36,6 +36,8 @@ namespace libtorrent::aux {
 		// create a part file at ``path``, that can hold ``num_pieces`` pieces.
 		// each piece being ``piece_size`` number of bytes
 		part_file(std::string path, std::string name, int num_pieces, int piece_size);
+		part_file(std::string root, std::string relative_path, std::string name
+			, int num_pieces, int piece_size);
 		~part_file();
 
 		int write(span<char const> buf, piece_index_t piece, int offset, error_code& ec);
@@ -49,6 +51,8 @@ namespace libtorrent::aux {
 		void free_piece(piece_index_t piece);
 
 		void move_partfile(std::string const& path, error_code& ec);
+		void move_partfile(std::string const& root
+			, std::string const& relative_path, error_code& ec);
 
 		// the function is called for every block of data belonging to the
 		// specified range that's in the part_file. The first parameter is the
@@ -73,7 +77,8 @@ namespace libtorrent::aux {
 		template <typename Hasher>
 		int do_hash(Hasher& ph, std::ptrdiff_t len, piece_index_t piece, int offset, error_code& ec);
 
-		std::string m_path;
+		std::string m_root;
+		std::string m_relative_path;
 		std::string const m_name;
 
 		// allocate a slot and return the slot index
diff --git a/include/libtorrent/aux_/path.hpp b/include/libtorrent/aux_/path.hpp
index 6c5c75f6e96f6115e17a6c1b9a343ebe954464a5..70c3784fc6b3e05be6836f814d11d15e47c78011 100644
--- a/include/libtorrent/aux_/path.hpp
+++ b/include/libtorrent/aux_/path.hpp
@@ -84,6 +84,32 @@ namespace libtorrent {
 	TORRENT_EXTRA_EXPORT bool is_directory(std::string const& f
 		, error_code& ec);
 
+	// Perform filesystem operations on an untrusted relative path while keeping
+	// resolution beneath a trusted root directory. On macOS these use the
+	// kernel's no-follow and resolve-beneath guarantees for the entire path.
+	TORRENT_EXTRA_EXPORT void confined_stat_file(std::string const& root
+		, std::string const& relative, file_status* s, error_code& ec);
+	TORRENT_EXTRA_EXPORT void confined_create_directories(std::string const& root
+		, std::string const& relative, error_code& ec);
+	TORRENT_EXTRA_EXPORT void confined_rename(std::string const& old_root
+		, std::string const& old_relative, std::string const& new_root
+		, std::string const& new_relative, error_code& ec);
+	TORRENT_EXTRA_EXPORT void confined_remove(std::string const& root
+		, std::string const& relative, bool directory, error_code& ec);
+	TORRENT_EXTRA_EXPORT bool confined_exists(std::string const& root
+		, std::string const& relative, error_code& ec);
+	TORRENT_EXTRA_EXPORT void confined_read_symlink(std::string const& root
+		, std::string const& relative, std::string& target, error_code& ec);
+	TORRENT_EXTRA_EXPORT void confined_symlink(std::string const& root
+		, std::string const& target, std::string const& relative, error_code& ec);
+
+#if !defined TORRENT_WINDOWS
+	namespace aux {
+		TORRENT_EXTRA_EXPORT int confined_open(std::string const& root
+			, std::string const& relative, int flags, int mode, error_code& ec);
+	}
+#endif
+
 	// file is expected to exist, link will be created to point to it. If hard
 	// links are not supported by the filesystem or OS, the file will be copied.
 	TORRENT_EXTRA_EXPORT void hard_link(std::string const& file
diff --git a/include/libtorrent/aux_/storage_utils.hpp b/include/libtorrent/aux_/storage_utils.hpp
index 574928a08a67d26dae898cdbc1859048b19d49c1..e081bec79bbf984e3b7759d331a2b20710ca23ce 100644
--- a/include/libtorrent/aux_/storage_utils.hpp
+++ b/include/libtorrent/aux_/storage_utils.hpp
@@ -81,9 +81,16 @@ namespace libtorrent::aux {
 
 	TORRENT_EXTRA_EXPORT void move_file(std::string const& f
 		, std::string const& newf, storage_error& se);
+	TORRENT_EXTRA_EXPORT void confined_move_file(std::string const& old_root
+		, std::string const& old_relative, std::string const& new_root
+		, std::string const& new_relative, storage_error& se);
 
 	TORRENT_EXTRA_EXPORT void copy_file(std::string const& f
 		, std::string const& newf, storage_error& se);
+
+	TORRENT_EXTRA_EXPORT void confined_copy_file(std::string const& old_root
+		, std::string const& old_relative, std::string const& new_root
+		, std::string const& new_relative, storage_error& se);
 }
 
 #endif
diff --git a/src/copy_file.cpp b/src/copy_file.cpp
index 9fd84c3a9f6fefa64182e66ed23abbbc4bfee9ba..7e1b7203111187f9cd0273aa34b015ca3a0efa05 100644
--- a/src/copy_file.cpp
+++ b/src/copy_file.cpp
@@ -231,6 +231,14 @@ void copy_file(std::string const& inf, std::string const& newf, storage_error& s
 	}
 }
 
+void confined_copy_file(std::string const& old_root
+	, std::string const& old_relative, std::string const& new_root
+	, std::string const& new_relative, storage_error& se)
+{
+	copy_file(combine_path(old_root, old_relative)
+		, combine_path(new_root, new_relative), se);
+}
+
 #else
 // Generic/linux implementation
 
@@ -315,6 +323,73 @@ ssize_t copy_range(int const fd_in, int const fd_out, off_t in_offset
 #endif
 }
 
+void copy_file_contents(int const infd, int const outfd
+	, struct stat const& in_stat, storage_error& se)
+{
+	bool const input_is_sparse = in_stat.st_size > off_t(in_stat.st_blocks) * 512;
+
+#if TORRENT_HAS_COPYFILE
+	if (!input_is_sparse)
+	{
+		copyfile_state_t state = copyfile_state_alloc();
+		if (fcopyfile(infd, outfd, state, COPYFILE_ALL) < 0)
+		{
+			se.operation = operation_t::file_copy;
+			se.ec.assign(errno, system_category());
+		}
+		copyfile_state_free(state);
+		return;
+	}
+#endif
+
+	if (::ftruncate(outfd, in_stat.st_size) < 0)
+	{
+		se.operation = operation_t::file_truncate;
+		se.ec.assign(errno, system_category());
+		return;
+	}
+
+#ifdef SEEK_HOLE
+	if (input_is_sparse)
+	{
+		copy_range_mode m;
+		ssize_t ret = 0;
+		off_t data_start = 0;
+		off_t data_end = 0;
+		for (;;)
+		{
+			data_start = ::lseek(infd, data_end, SEEK_DATA);
+			if (data_start == off_t(-1))
+			{
+				int const err = errno;
+				if (err == ENXIO) return;
+				if (err == ENOTSUP) break;
+				se.operation = operation_t::file_seek;
+				se.ec.assign(err, system_category());
+				return;
+			}
+
+			data_end = ::lseek(infd, data_start, SEEK_HOLE);
+			if (data_end == off_t(-1))
+			{
+				int const err = errno;
+				if (err == ENOTSUP) break;
+				se.operation = operation_t::file_seek;
+				se.ec.assign(err, system_category());
+				return;
+			}
+
+			ret = copy_range(infd, outfd, data_start, data_end - data_start, &m, se);
+			if (ret <= 0) return;
+			if (data_end == in_stat.st_size) return;
+		}
+	}
+#endif
+
+	copy_range_mode m;
+	copy_range(infd, outfd, 0, in_stat.st_size, &m, se);
+}
+
 } // anonymous namespace
 
 void copy_file(std::string const& inf, std::string const& newf, storage_error& se)
@@ -360,78 +435,67 @@ void copy_file(std::string const& inf, std::string const& newf, storage_error& s
 		return;
 	}
 
-#if TORRENT_HAS_COPYFILE
-	if (!input_is_sparse)
+	copy_file_contents(infd.fd(), outfd.fd(), in_stat, se);
+}
+
+void confined_copy_file(std::string const& old_root
+	, std::string const& old_relative, std::string const& new_root
+	, std::string const& new_relative, storage_error& se)
+{
+	se.ec.clear();
+	int read_flags = O_RDONLY;
+#ifdef O_CLOEXEC
+	read_flags |= O_CLOEXEC;
+#endif
+	error_code open_error;
+	aux::file_descriptor const infd = aux::confined_open(old_root, old_relative
+		, read_flags, 0, open_error);
+	if (infd.fd() < 0)
 	{
-		// the the file isn't sparse use the system copy function (which
-		// expands sparse regions)
-		// this only works on 10.5
-		copyfile_state_t state = copyfile_state_alloc();
-		if (fcopyfile(infd.fd(), outfd.fd(), state, COPYFILE_ALL) < 0)
+		std::string link_target;
+		error_code link_error;
+		confined_read_symlink(old_root, old_relative, link_target, link_error);
+		if (!link_error)
 		{
-			se.operation = operation_t::file_copy;
-			se.ec.assign(errno, system_category());
+			confined_symlink(new_root, link_target, new_relative, link_error);
+			if (link_error)
+			{
+				se.operation = operation_t::file_copy;
+				se.ec = link_error;
+			}
+			return;
 		}
-		copyfile_state_free(state);
+		se.operation = operation_t::file_stat;
+		se.ec = open_error;
 		return;
 	}
-#endif
 
-	if (::ftruncate(outfd.fd(), in_stat.st_size) < 0)
+	struct stat in_stat;
+	if (::fstat(infd.fd(), &in_stat) != 0)
 	{
-		se.operation = operation_t::file_truncate;
+		se.operation = operation_t::file_stat;
 		se.ec.assign(errno, system_category());
 		return;
 	}
 
-#ifdef SEEK_HOLE
-	if (input_is_sparse)
+	bool const input_is_sparse = in_stat.st_size > off_t(in_stat.st_blocks) * 512;
+	int write_flags = O_RDWR | O_CREAT | (input_is_sparse ? O_TRUNC : 0);
+#ifdef O_CLOEXEC
+	write_flags |= O_CLOEXEC;
+#endif
+	aux::file_descriptor const outfd = aux::confined_open(new_root, new_relative
+		, write_flags, in_stat.st_mode, open_error);
+	if (outfd.fd() < 0)
 	{
-		copy_range_mode m;
-		ssize_t ret = 0;
-		off_t data_start = 0;
-		off_t data_end = 0;
-		for (;;)
-		{
-			data_start = ::lseek(infd.fd(), data_end, SEEK_DATA);
-			if (data_start == off_t(-1))
-			{
-				int const err = errno;
-				// if we SEEK_DATA while the file location is past the last
-				// non-sparse region (i.e. the file has been truncated without
-				// filled in, the end of the file may be a sparse region). In
-				// this case there's nothing left to copy, and we're done
-				if (err == ENXIO) return;
-				// if SEEK_DATA is not supported, fall back to plain copy
-				if (err == ENOTSUP) break;
-				se.operation = operation_t::file_seek;
-				se.ec.assign(err, system_category());
-				return;
-			}
-
-			data_end = ::lseek(infd.fd(), data_start, SEEK_HOLE);
-			if (data_end == off_t(-1))
-			{
-				int const err = errno;
-				if (err == ENOTSUP) break;
-				se.operation = operation_t::file_seek;
-				se.ec.assign(err, system_category());
-				return;
-			}
-
-			ret = copy_range(infd.fd(), outfd.fd(), data_start, data_end - data_start, &m, se);
-			if (ret <= 0) return;
-			if (data_end == in_stat.st_size) return;
-		}
+		se.operation = operation_t::file_open;
+		se.ec = open_error;
+		return;
 	}
-#endif
 
-	copy_range_mode m;
-	copy_range(infd.fd(), outfd.fd(), 0, in_stat.st_size, &m, se);
+	copy_file_contents(infd.fd(), outfd.fd(), in_stat, se);
 }
 
 #endif // TORRENT_WINDOWS
 
 }
 }
-
diff --git a/src/file.cpp b/src/file.cpp
index 5cbb28ed212ba3fb7b4a25de63c5746580decedb..9724c0acf71d1605f521118cb04d3ab96409ce3a 100644
--- a/src/file.cpp
+++ b/src/file.cpp
@@ -536,6 +536,24 @@ typedef struct _FILE_ALLOCATED_RANGE_BUFFER {
 		if (ret < 0) throw_ex<storage_error>(error_code(errno, system_category()), operation_t::file_open);
 		return ret;
 	}
+
+	int open_file(std::string const& root, std::string const& relative
+		, open_mode_t const mode)
+	{
+		error_code ec;
+		int ret = aux::confined_open(root, relative, file_flags(mode)
+			, file_perms(mode), ec);
+
+#ifdef O_NOATIME
+		if (ret < 0 && (mode & open_mode::no_atime))
+		{
+			ret = aux::confined_open(root, relative
+				, file_flags(mode & ~open_mode::no_atime), file_perms(mode), ec);
+		}
+#endif
+		if (ret < 0) throw_ex<storage_error>(ec, operation_t::file_open);
+		return ret;
+	}
 #endif // TORRENT_WINDOWS
 
 } // anonymous namespace
@@ -581,11 +599,24 @@ file_handle::file_handle(string_view name, std::int64_t const size
 	}
 }
 
+file_handle::file_handle(string_view const root, string_view const relative
+	, std::int64_t const size, open_mode_t const mode)
+	: file_handle(combine_path(root, relative), size, mode)
+{}
+
 #else
 
 file_handle::file_handle(string_view name, std::int64_t const size
 	, open_mode_t const mode)
-	: m_fd(open_file(convert_to_native_path_string(std::string(name)), mode))
+	: file_handle({}, name, size, mode)
+{}
+
+file_handle::file_handle(string_view const root, string_view const relative
+	, std::int64_t const size
+	, open_mode_t const mode)
+	: m_fd(root.empty()
+		? open_file(convert_to_native_path_string(std::string(relative)), mode)
+		: open_file(std::string(root), std::string(relative), mode))
 {
 #ifdef DIRECTIO_ON
 	// for solaris
diff --git a/src/file_pool_impl.cpp b/src/file_pool_impl.cpp
index 0ebc9725b641b1864d1112036484abd616c19ce5..9e99d69c20f66d048147c736b312fc96eed6f152 100644
--- a/src/file_pool_impl.cpp
+++ b/src/file_pool_impl.cpp
@@ -249,14 +249,14 @@ namespace libtorrent::aux {
 #endif
 		)
 	{
-		std::string const file_path = fn.file_path(file_index, p);
+		std::string const file_path = fn.file_path(file_index);
 #if TORRENT_HAVE_MAP_VIEW_OF_FILE
 		typename FileEntry::lock_type lou(*open_unmap_lock);
 		TORRENT_UNUSED(lou);
 #endif
 		try
 		{
-			return FileEntry(file_key, file_path, m, fn.file_size(file_index)
+			return FileEntry(file_key, p, file_path, m, fn.file_size(file_index)
 #if TORRENT_HAVE_MAP_VIEW_OF_FILE
 				, open_unmap_lock
 #endif
@@ -284,7 +284,10 @@ namespace libtorrent::aux {
 			// this means the directory the file is in doesn't exist.
 			// so create it
 			se.ec.clear();
-			create_directories(parent_path(fn.file_path(file_index, p)), se.ec);
+			create_directories(p, se.ec);
+			if (se.ec)
+				throw_ex<storage_error>(se);
+			confined_create_directories(p, parent_path(file_path), se.ec);
 
 			if (se.ec)
 			{
@@ -293,7 +296,7 @@ namespace libtorrent::aux {
 				throw_ex<storage_error>(se);
 			}
 
-			return FileEntry(file_key, file_path, m, fn.file_size(file_index)
+			return FileEntry(file_key, p, file_path, m, fn.file_size(file_index)
 #if TORRENT_HAVE_MAP_VIEW_OF_FILE
 				, open_unmap_lock
 #endif
@@ -454,4 +457,3 @@ namespace libtorrent::aux {
 	template struct file_pool_impl<file_pool_entry>;
 #endif
 }
-
diff --git a/src/mmap_storage.cpp b/src/mmap_storage.cpp
index cb51cda332a9409419906b3d762302221e07ce7a..bdf7ef3ad72a0ebe304534a9d0d1b811393bae8d 100644
--- a/src/mmap_storage.cpp
+++ b/src/mmap_storage.cpp
@@ -150,7 +150,8 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 		if (m_part_file) return;
 
 		m_part_file = std::make_unique<part_file>(
-			m_part_file_dir.empty() ? m_save_path : combine_path(m_save_path, m_part_file_dir)
+			m_save_path
+			, m_part_file_dir
 			, m_part_file_name
 			, files().num_pieces(), files().piece_length());
 	}
@@ -249,8 +250,8 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 				// this is not implemented yet.
 				// so we just don't use a partfile for this file
 
-				std::string const fp = fs.file_path(i, m_save_path);
-				bool const file_exists = exists(fp, ec.ec);
+				bool const file_exists = confined_exists(
+					m_save_path, fs.file_path(i), ec.ec);
 				if (ec.ec)
 				{
 					ec.file(i);
@@ -375,7 +376,21 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 		aux::initialize_storage(fs, m_save_path, m_stat_cache, m_file_priority
 			, [&sett, this](file_index_t const file_index, storage_error& e)
 			{ open_file(sett, file_index, aux::open_mode::write, e); }
-			, aux::create_symlink
+			, [this](std::string const& target, std::string const& link
+				, storage_error& e)
+			{
+				error_code relative_error;
+				std::string const relative = lexically_relative_normal(
+					m_save_path, link, relative_error);
+				if (relative_error)
+				{
+					e.ec = relative_error;
+					e.operation = operation_t::symlink;
+					return;
+				}
+				confined_symlink(m_save_path, target, relative, e.ec);
+				if (e.ec) e.operation = operation_t::symlink;
+			}
 			, [&ret](file_index_t, std::int64_t) { ret |= disk_status::oversized_file; }
 			, ec);
 
@@ -394,9 +409,8 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 		if (ec) return false;
 
 		file_status s;
-		stat_file(combine_path(
-			m_part_file_dir.empty() ? m_save_path : combine_path(m_save_path, m_part_file_dir)
-			, m_part_file_name), &s, ec.ec);
+		confined_stat_file(m_save_path
+			, combine_path(m_part_file_dir, m_part_file_name), &s, ec.ec);
 		if (!ec) return true;
 
 		// the part file not existing is expected
@@ -415,7 +429,25 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 		, storage_error& ec)
 	{
 		if (index < file_index_t(0) || index >= files().end_file()) return;
-		std::string const old_name = m_renamed_files.file_path(files(), index, m_save_path);
+		if (m_renamed_files.file_absolute_path(files(), index)
+			|| is_complete(new_filename))
+		{
+			ec.ec = make_error_code(boost::system::errc::invalid_argument);
+			ec.file(index);
+			ec.operation = operation_t::file_rename;
+			return;
+		}
+		error_code destination_error;
+		confined_exists(m_save_path, new_filename, destination_error);
+		if (destination_error)
+		{
+			ec.ec = destination_error;
+			ec.file(index);
+			ec.operation = operation_t::file_rename;
+			return;
+		}
+
+		std::string const old_name = m_renamed_files.file_path(files(), index);
 		m_pool.release(storage_index(), index);
 
 		// if the old file doesn't exist, just succeed and change the filename
@@ -425,15 +457,10 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 		// fail later when we try to write to the file the first time, because
 		// the user then will have had a chance to make the destination directory
 		// valid.
-		if (exists(old_name, ec.ec))
+		if (confined_exists(m_save_path, old_name, ec.ec))
 		{
-			std::string new_path;
-			if (is_complete(new_filename)) new_path = new_filename;
-			else new_path = combine_path(m_save_path, new_filename);
-			std::string new_dir = parent_path(new_path);
-
 			error_code best_effort;
-			if (exists(new_path, best_effort))
+			if (confined_exists(m_save_path, new_filename, best_effort))
 			{
 				// We don't want to overwrite an existing file
 				ec.ec = error_code(boost::system::errc::file_exists, generic_category());
@@ -444,7 +471,8 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 
 			// create any missing directories that the new filename
 			// lands in
-			create_directories(new_dir, ec.ec);
+			confined_create_directories(m_save_path
+				, parent_path(new_filename), ec.ec);
 			if (ec.ec)
 			{
 				ec.file(index);
@@ -452,7 +480,8 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 				return;
 			}
 
-			rename(old_name, new_path, ec.ec);
+			confined_rename(m_save_path, old_name
+				, m_save_path, new_filename, ec.ec);
 
 			// if old_name doesn't exist, that's not an error
 			// here. Once we start writing to the file, it will
@@ -463,7 +492,8 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 			if (ec)
 			{
 				ec.ec.clear();
-				aux::copy_file(old_name, new_path, ec);
+				aux::confined_copy_file(m_save_path, old_name
+					, m_save_path, new_filename, ec);
 
 				if (ec)
 				{
@@ -472,7 +502,7 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 				}
 
 				error_code ignore;
-				remove(old_name, ignore);
+				confined_remove(m_save_path, old_name, false, ignore);
 			}
 		}
 		else if (ec.ec)
@@ -542,18 +572,7 @@ mmap_storage::mmap_storage(storage_params const& params, aux::file_view_pool& po
 		auto move_partfile = [&](std::string const& new_save_path, error_code& e)
 		{
 			if (!m_part_file) return;
-			std::string new_part_file_dir;
-			if (!m_part_file_dir.empty())
-			{
-				new_part_file_dir = combine_path(new_save_path, m_part_file_dir);
-				create_directories(new_part_file_dir, e);
-				if (e) return;
-			}
-			else
-			{
-				new_part_file_dir = new_save_path;
-			}
-			m_part_file->move_partfile(new_part_file_dir, e);
+			m_part_file->move_partfile(new_save_path, m_part_file_dir, e);
 		};
 		std::tie(ret, m_save_path) = aux::move_storage(
 			names()
diff --git a/src/part_file.cpp b/src/part_file.cpp
index c6e5fe5d11b53f9a1edcfa457e7d716288ad1830..962f454a27c36d9609223a82af9112020972915d 100644
--- a/src/part_file.cpp
+++ b/src/part_file.cpp
@@ -60,7 +60,13 @@ namespace libtorrent::aux {
 
 	part_file::part_file(std::string path, std::string name
 		, int const num_pieces, int const piece_size)
-		: m_path(std::move(path))
+		: part_file(std::move(path), {}, std::move(name), num_pieces, piece_size)
+	{}
+
+	part_file::part_file(std::string root, std::string relative_path
+		, std::string name, int const num_pieces, int const piece_size)
+		: m_root(std::move(root))
+		, m_relative_path(std::move(relative_path))
 		, m_name(std::move(name))
 		, m_max_pieces(num_pieces)
 		, m_piece_size(piece_size)
@@ -259,9 +265,9 @@ namespace libtorrent::aux {
 
 	aux::file_handle part_file::open_file(aux::open_mode_t const mode, error_code& ec) try
 	{
-		std::string const fn = combine_path(m_path, m_name);
+		std::string const relative = combine_path(m_relative_path, m_name);
 		try {
-			return {fn, 0, mode};
+			return {m_root, relative, 0, mode};
 		}
 		catch (storage_error const& e)
 		{
@@ -271,10 +277,14 @@ namespace libtorrent::aux {
 				// this means the directory the file is in doesn't exist.
 				// so create it
 				ec.clear();
-				create_directories(m_path, ec);
+				create_directories(m_root, ec);
+				if (ec) return {};
+				confined_create_directories(m_root, m_relative_path, ec);
 				if (ec) return {};
-				return {fn, 0, mode};
+				return {m_root, relative, 0, mode};
 			}
+			if (e.ec != boost::system::errc::no_such_file_or_directory)
+				ec = e.ec;
 			return {};
 		}
 	}
@@ -302,6 +312,12 @@ namespace libtorrent::aux {
 	}
 
 	void part_file::move_partfile(std::string const& path, error_code& ec)
+	{
+		move_partfile(path, {}, ec);
+	}
+
+	void part_file::move_partfile(std::string const& root
+		, std::string const& relative_path, error_code& ec)
 	{
 		std::lock_guard<std::mutex> l(m_mutex);
 
@@ -310,23 +326,28 @@ namespace libtorrent::aux {
 
 		if (!m_piece_map.empty())
 		{
-			std::string old_path = combine_path(m_path, m_name);
-			std::string new_path = combine_path(path, m_name);
-
-			rename(old_path, new_path, ec);
+			std::string const old_path = combine_path(m_relative_path, m_name);
+			std::string const new_path = combine_path(relative_path, m_name);
+
+			create_directories(root, ec);
+			if (ec) return;
+			confined_create_directories(root, relative_path, ec);
+			if (ec) return;
+			confined_rename(m_root, old_path, root, new_path, ec);
 			if (ec == boost::system::errc::no_such_file_or_directory)
 				ec.clear();
 
 			if (ec)
 			{
 				storage_error se;
-				aux::copy_file(old_path, new_path, se);
+				aux::confined_copy_file(m_root, old_path, root, new_path, se);
 				ec = se.ec;
 				if (ec) return;
-				remove(old_path, ec);
+				confined_remove(m_root, old_path, false, ec);
 			}
 		}
-		m_path = path;
+		m_root = root;
+		m_relative_path = relative_path;
 	}
 
 	void part_file::export_file(std::function<void(std::int64_t, span<char>)> f
@@ -413,8 +434,8 @@ namespace libtorrent::aux {
 		{
 			// if we don't have any pieces left in the
 			// part file, remove it
-			std::string const p = combine_path(m_path, m_name);
-			remove(p, ec);
+			std::string const p = combine_path(m_relative_path, m_name);
+			confined_remove(m_root, p, false, ec);
 
 			if (ec == boost::system::errc::no_such_file_or_directory)
 				ec.clear();
diff --git a/src/path.cpp b/src/path.cpp
index 59b829db20f26901f930df918c9ca991679d4285..01c86ed0d4fb6669ca3198ea2661dd2a5544e8ed 100644
--- a/src/path.cpp
+++ b/src/path.cpp
@@ -46,6 +46,9 @@ see LICENSE file.
 #include "libtorrent/aux_/alloca.hpp"
 #include "libtorrent/aux_/path.hpp"
 #include "libtorrent/aux_/directory.hpp"
+#ifndef TORRENT_WINDOWS
+#include "libtorrent/aux_/file_descriptor.hpp"
+#endif
 #include "libtorrent/aux_/string_util.hpp"
 #include <cstring>
 #include <algorithm> // for std::replace
@@ -60,6 +63,10 @@ see LICENSE file.
 #include <sys/stat.h>
 #include <climits> // for IOV_MAX
 
+#if defined __APPLE__ && defined __MACH__
+#include <sys/stdio.h> // for renameatx_np
+#endif
+
 #ifdef TORRENT_WINDOWS
 // windows part
 
@@ -117,6 +124,55 @@ namespace {
 		return std::unique_ptr<T, free_function>(ptr, free_function{});
 	}
 
+#if defined __APPLE__ && defined __MACH__
+	bool valid_relative_path(string_view const path, bool const allow_empty
+		, bool const allow_trailing_separator = false)
+	{
+		if (path.empty()) return allow_empty;
+		if (path.front() == '/') return false;
+
+		std::size_t start = 0;
+		while (start < path.size())
+		{
+			auto const end = path.find('/', start);
+			auto const component = path.substr(start
+				, end == string_view::npos ? string_view::npos : end - start);
+			if (component.empty() || component == "." || component == "..")
+				return false;
+			if (end == string_view::npos) return true;
+			start = end + 1;
+			if (start == path.size()) return allow_trailing_separator;
+		}
+		return true;
+	}
+
+	void invalid_relative_path(error_code& ec)
+	{
+		ec = make_error_code(boost::system::errc::invalid_argument);
+	}
+
+	aux::file_descriptor open_confined_root(std::string const& root, error_code& ec)
+	{
+		auto const native_root = convert_to_native_path_string(root);
+		int const fd = ::open(native_root.c_str(), O_SEARCH | O_CLOEXEC);
+		if (fd < 0) ec.assign(errno, system_category());
+		return aux::file_descriptor(fd);
+	}
+
+	void fill_posix_file_status(file_status& s, struct ::stat const& ret)
+	{
+		static_assert(sizeof(ret.st_size) >= 8, "64 bit file operations are required");
+
+		s.file_size = ret.st_size;
+		s.atime = std::uint64_t(ret.st_atime);
+		s.mtime = std::uint64_t(ret.st_mtime);
+		s.ctime = std::uint64_t(ret.st_ctime);
+		s.mode = (S_ISDIR(ret.st_mode) ? file_status::directory : file_attributes_t{})
+			| (S_ISLNK(ret.st_mode) ? file_status::symlink : file_attributes_t{})
+			| ((ret.st_mode & S_IXUSR) ? file_status::executable : file_attributes_t{});
+	}
+#endif
+
 #ifdef TORRENT_WINDOWS
 	time_t file_time_to_posix(FILETIME f)
 	{
@@ -330,6 +386,311 @@ namespace {
 #undef RenameFunction_
 	}
 
+	void confined_stat_file(std::string const& root, std::string const& relative
+		, file_status* const s, error_code& ec)
+	{
+		ec.clear();
+#if defined __APPLE__ && defined __MACH__
+		if (!valid_relative_path(relative, false))
+		{
+			invalid_relative_path(ec);
+			return;
+		}
+
+		auto root_fd = open_confined_root(root, ec);
+		if (ec) return;
+
+		struct ::stat ret{};
+		if (::fstatat(root_fd.fd(), relative.c_str(), &ret
+			, AT_SYMLINK_NOFOLLOW_ANY | AT_RESOLVE_BENEATH) < 0)
+		{
+			ec.assign(errno, system_category());
+			return;
+		}
+		if (S_ISLNK(ret.st_mode))
+		{
+			ec.assign(ELOOP, system_category());
+			return;
+		}
+		fill_posix_file_status(*s, ret);
+#else
+		stat_file(combine_path(root, relative), s, ec);
+#endif
+	}
+
+	void confined_create_directories(std::string const& root
+		, std::string const& relative, error_code& ec)
+	{
+		ec.clear();
+#if defined __APPLE__ && defined __MACH__
+		if (relative.empty()) return;
+		if (!valid_relative_path(relative, false, true))
+		{
+			invalid_relative_path(ec);
+			return;
+		}
+
+		auto directory = open_confined_root(root, ec);
+		if (ec) return;
+
+		std::size_t start = 0;
+		while (start < relative.size())
+		{
+			auto const end = relative.find('/', start);
+			std::string const component = relative.substr(start
+				, end == std::string::npos ? std::string::npos : end - start);
+			if (::mkdirat(directory.fd(), component.c_str(), 0777) < 0
+				&& errno != EEXIST)
+			{
+				ec.assign(errno, system_category());
+				return;
+			}
+
+			int const next_fd = ::openat(directory.fd(), component.c_str()
+				, O_SEARCH | O_CLOEXEC | O_NOFOLLOW_ANY | O_RESOLVE_BENEATH);
+			if (next_fd < 0)
+			{
+				ec.assign(errno, system_category());
+				return;
+			}
+			directory = aux::file_descriptor(next_fd);
+			if (end == std::string::npos) return;
+			start = end + 1;
+		}
+#else
+		create_directories(combine_path(root, relative), ec);
+#endif
+	}
+
+	void confined_rename(std::string const& old_root
+		, std::string const& old_relative, std::string const& new_root
+		, std::string const& new_relative, error_code& ec)
+	{
+		ec.clear();
+#if defined __APPLE__ && defined __MACH__
+		if (!valid_relative_path(old_relative, false)
+			|| !valid_relative_path(new_relative, false))
+		{
+			invalid_relative_path(ec);
+			return;
+		}
+
+		auto old_root_fd = open_confined_root(old_root, ec);
+		if (ec) return;
+		auto new_root_fd = open_confined_root(new_root, ec);
+		if (ec) return;
+
+		if (::renameatx_np(old_root_fd.fd(), old_relative.c_str()
+			, new_root_fd.fd(), new_relative.c_str()
+			, RENAME_NOFOLLOW_ANY | RENAME_RESOLVE_BENEATH) < 0)
+			ec.assign(errno, system_category());
+#else
+		rename(combine_path(old_root, old_relative)
+			, combine_path(new_root, new_relative), ec);
+#endif
+	}
+
+	void confined_remove(std::string const& root, std::string const& relative
+		, bool const directory, error_code& ec)
+	{
+		ec.clear();
+#if defined __APPLE__ && defined __MACH__
+		if (!valid_relative_path(relative, false, true))
+		{
+			invalid_relative_path(ec);
+			return;
+		}
+
+		auto root_fd = open_confined_root(root, ec);
+		if (ec) return;
+		int const flags = AT_SYMLINK_NOFOLLOW_ANY | AT_RESOLVE_BENEATH
+			| (directory ? AT_REMOVEDIR : 0);
+		if (::unlinkat(root_fd.fd(), relative.c_str(), flags) < 0)
+			ec.assign(errno, system_category());
+#else
+		TORRENT_UNUSED(directory);
+		remove(combine_path(root, relative), ec);
+#endif
+	}
+
+	bool confined_exists(std::string const& root, std::string const& relative
+		, error_code& ec)
+	{
+		file_status s;
+		confined_stat_file(root, relative, &s, ec);
+		if (ec)
+		{
+			if (ec == boost::system::errc::no_such_file_or_directory
+				|| ec == boost::system::errc::filename_too_long)
+				ec.clear();
+			return false;
+		}
+		return true;
+	}
+
+	void confined_read_symlink(std::string const& root
+		, std::string const& relative, std::string& target, error_code& ec)
+	{
+		ec.clear();
+		target.clear();
+#if defined __APPLE__ && defined __MACH__ && TORRENT_HAS_SYMLINK
+		if (!valid_relative_path(relative, false))
+		{
+			invalid_relative_path(ec);
+			return;
+		}
+
+		auto root_fd = open_confined_root(root, ec);
+		if (ec) return;
+		std::string const parent = parent_path(relative);
+		int parent_fd = root_fd.fd();
+		aux::file_descriptor opened_parent(-1);
+		if (!parent.empty())
+		{
+			int const fd = ::openat(root_fd.fd(), parent.c_str()
+				, O_SEARCH | O_CLOEXEC | O_NOFOLLOW_ANY | O_RESOLVE_BENEATH);
+			if (fd < 0)
+			{
+				ec.assign(errno, system_category());
+				return;
+			}
+			opened_parent = aux::file_descriptor(fd);
+			parent_fd = opened_parent.fd();
+		}
+
+		std::vector<char> buffer(PATH_MAX);
+		auto const ret = ::readlinkat(parent_fd, filename(relative).c_str()
+			, buffer.data(), buffer.size());
+		if (ret < 0)
+		{
+			ec.assign(errno, system_category());
+			return;
+		}
+		if (std::size_t(ret) == buffer.size())
+		{
+			ec = make_error_code(boost::system::errc::filename_too_long);
+			return;
+		}
+		target.assign(buffer.data(), std::size_t(ret));
+#elif TORRENT_HAS_SYMLINK
+		std::vector<char> buffer(PATH_MAX);
+		auto const link = combine_path(root, relative);
+		auto const ret = ::readlink(link.c_str(), buffer.data(), buffer.size());
+		if (ret < 0)
+		{
+			ec.assign(errno, system_category());
+			return;
+		}
+		if (std::size_t(ret) == buffer.size())
+		{
+			ec = make_error_code(boost::system::errc::filename_too_long);
+			return;
+		}
+		target.assign(buffer.data(), std::size_t(ret));
+#else
+		TORRENT_UNUSED(root);
+		TORRENT_UNUSED(relative);
+#endif
+	}
+
+	void confined_symlink(std::string const& root, std::string const& target
+		, std::string const& relative, error_code& ec)
+	{
+		ec.clear();
+#if defined __APPLE__ && defined __MACH__ && TORRENT_HAS_SYMLINK
+		if (!valid_relative_path(relative, false))
+		{
+			invalid_relative_path(ec);
+			return;
+		}
+
+		std::string const parent = parent_path(relative);
+		create_directories(root, ec);
+		if (ec) return;
+		confined_create_directories(root, parent, ec);
+		if (ec) return;
+
+		auto root_fd = open_confined_root(root, ec);
+		if (ec) return;
+		int parent_fd = root_fd.fd();
+		aux::file_descriptor opened_parent(-1);
+		if (!parent.empty())
+		{
+			int const fd = ::openat(root_fd.fd(), parent.c_str()
+				, O_SEARCH | O_CLOEXEC | O_NOFOLLOW_ANY | O_RESOLVE_BENEATH);
+			if (fd < 0)
+			{
+				ec.assign(errno, system_category());
+				return;
+			}
+			opened_parent = aux::file_descriptor(fd);
+			parent_fd = opened_parent.fd();
+		}
+
+		std::string const leaf = filename(relative);
+		if (::symlinkat(target.c_str(), parent_fd, leaf.c_str()) == 0) return;
+		int const error = errno;
+		if (error != EEXIST)
+		{
+			ec.assign(error, generic_category());
+			return;
+		}
+
+		char buffer[512];
+		auto const ret = ::readlinkat(parent_fd, leaf.c_str(), buffer, sizeof(buffer));
+		if (ret <= 0 || target != string_view(buffer, std::size_t(ret)))
+			ec.assign(error, generic_category());
+#elif TORRENT_HAS_SYMLINK
+		std::string const link = combine_path(root, relative);
+		create_directories(parent_path(link), ec);
+		if (ec) return;
+		if (::symlink(target.c_str(), link.c_str()) == 0) return;
+		int const error = errno;
+		if (error != EEXIST)
+		{
+			ec.assign(error, generic_category());
+			return;
+		}
+		char buffer[512];
+		auto const ret = ::readlink(link.c_str(), buffer, sizeof(buffer));
+		if (ret <= 0 || target != string_view(buffer, std::size_t(ret)))
+			ec.assign(error, generic_category());
+#else
+		TORRENT_UNUSED(root);
+		TORRENT_UNUSED(target);
+		TORRENT_UNUSED(relative);
+		TORRENT_UNUSED(ec);
+#endif
+	}
+
+#if !defined TORRENT_WINDOWS
+	int aux::confined_open(std::string const& root, std::string const& relative
+		, int const flags, int const mode, error_code& ec)
+	{
+		ec.clear();
+#if defined __APPLE__ && defined __MACH__
+		if (!valid_relative_path(relative, false))
+		{
+			invalid_relative_path(ec);
+			return -1;
+		}
+		auto root_fd = open_confined_root(root, ec);
+		if (ec) return -1;
+		int open_flags = flags | O_NOFOLLOW_ANY | O_RESOLVE_BENEATH;
+		if ((flags & O_ACCMODE) != O_RDONLY) open_flags |= O_UNIQUE;
+		int const fd = ::openat(root_fd.fd(), relative.c_str()
+			, open_flags, mode);
+		if (fd < 0) ec.assign(errno, system_category());
+		return fd;
+#else
+		auto const path = convert_to_native_path_string(combine_path(root, relative));
+		int const fd = ::open(path.c_str(), flags, mode);
+		if (fd < 0) ec.assign(errno, system_category());
+		return fd;
+#endif
+	}
+#endif
+
 	void create_directories(std::string const& f, error_code& ec)
 	{
 		ec.clear();
diff --git a/src/pread_storage.cpp b/src/pread_storage.cpp
index c6e7fa9779990069593c44c60b8f000c0f28fc3c..9e0af24f1517117fd59a1206cec8d1d3a3d68800 100644
--- a/src/pread_storage.cpp
+++ b/src/pread_storage.cpp
@@ -101,7 +101,8 @@ namespace libtorrent::aux {
 		if (m_part_file) return;
 
 		m_part_file = std::make_unique<part_file>(
-			m_part_file_dir.empty() ? m_save_path : combine_path(m_save_path, m_part_file_dir)
+			m_save_path
+			, m_part_file_dir
 			, m_part_file_name
 			, files().num_pieces(), files().piece_length());
 	}
@@ -172,8 +173,8 @@ namespace libtorrent::aux {
 				// this is not implemented yet.
 				// so we just don't use a partfile for this file
 
-				std::string const fp = fs.file_path(i, m_save_path);
-				bool const file_exists = exists(fp, ec.ec);
+				bool const file_exists = confined_exists(
+					m_save_path, fs.file_path(i), ec.ec);
 				if (ec.ec)
 				{
 					ec.file(i);
@@ -267,7 +268,21 @@ namespace libtorrent::aux {
 		initialize_storage(fs, m_save_path, m_stat_cache, m_file_priority
 			, [&sett, this](file_index_t const file_index, storage_error& e)
 			{ open_file(sett, file_index, open_mode::write, e); }
-			, create_symlink
+			, [this](std::string const& target, std::string const& link
+				, storage_error& e)
+			{
+				error_code relative_error;
+				std::string const relative = lexically_relative_normal(
+					m_save_path, link, relative_error);
+				if (relative_error)
+				{
+					e.ec = relative_error;
+					e.operation = operation_t::symlink;
+					return;
+				}
+				confined_symlink(m_save_path, target, relative, e.ec);
+				if (e.ec) e.operation = operation_t::symlink;
+			}
 			, [&ret](file_index_t, std::int64_t) { ret = ret | disk_status::oversized_file; }
 			, ec);
 
@@ -286,10 +301,8 @@ namespace libtorrent::aux {
 		if (ec) return false;
 
 		file_status s;
-		std::string const part_file = combine_path(
-			m_part_file_dir.empty() ? m_save_path : combine_path(m_save_path, m_part_file_dir)
-			, m_part_file_name);
-		stat_file(part_file, &s, ec.ec);
+		std::string const part_file = combine_path(m_part_file_dir, m_part_file_name);
+		confined_stat_file(m_save_path, part_file, &s, ec.ec);
 		if (!ec) return true;
 
 		// the part file not existing is expected
@@ -308,7 +321,25 @@ namespace libtorrent::aux {
 		, storage_error& ec)
 	{
 		if (index < file_index_t(0) || index >= files().end_file()) return;
-		std::string const old_name = m_renamed_files.file_path(files(), index, m_save_path);
+		if (m_renamed_files.file_absolute_path(files(), index)
+			|| is_complete(new_filename))
+		{
+			ec.ec = make_error_code(boost::system::errc::invalid_argument);
+			ec.file(index);
+			ec.operation = operation_t::file_rename;
+			return;
+		}
+		error_code destination_error;
+		confined_exists(m_save_path, new_filename, destination_error);
+		if (destination_error)
+		{
+			ec.ec = destination_error;
+			ec.file(index);
+			ec.operation = operation_t::file_rename;
+			return;
+		}
+
+		std::string const old_name = m_renamed_files.file_path(files(), index);
 		m_pool.release(storage_index(), index);
 
 		// if the old file doesn't exist, just succeed and change the filename
@@ -318,16 +349,12 @@ namespace libtorrent::aux {
 		// fail later when we try to write to the file the first time, because
 		// the user then will have had a chance to make the destination directory
 		// valid.
-		if (exists(old_name, ec.ec))
+		if (confined_exists(m_save_path, old_name, ec.ec))
 		{
-			std::string new_path;
-			if (is_complete(new_filename)) new_path = new_filename;
-			else new_path = combine_path(m_save_path, new_filename);
-			std::string new_dir = parent_path(new_path);
-
 			// create any missing directories that the new filename
 			// lands in
-			create_directories(new_dir, ec.ec);
+			confined_create_directories(m_save_path
+				, parent_path(new_filename), ec.ec);
 			if (ec.ec)
 			{
 				ec.file(index);
@@ -335,7 +362,8 @@ namespace libtorrent::aux {
 				return;
 			}
 
-			rename(old_name, new_path, ec.ec);
+			confined_rename(m_save_path, old_name
+				, m_save_path, new_filename, ec.ec);
 
 			// if old_name doesn't exist, that's not an error
 			// here. Once we start writing to the file, it will
@@ -346,7 +374,8 @@ namespace libtorrent::aux {
 			if (ec)
 			{
 				ec.ec.clear();
-				copy_file(old_name, new_path, ec);
+				confined_copy_file(m_save_path, old_name
+					, m_save_path, new_filename, ec);
 
 				if (ec)
 				{
@@ -355,7 +384,7 @@ namespace libtorrent::aux {
 				}
 
 				error_code ignore;
-				remove(old_name, ignore);
+				confined_remove(m_save_path, old_name, false, ignore);
 			}
 		}
 		else if (ec.ec)
@@ -424,18 +453,7 @@ namespace libtorrent::aux {
 		auto move_partfile = [&](std::string const& new_save_path, error_code& e)
 		{
 			if (!m_part_file) return;
-			std::string new_part_file_dir;
-			if (!m_part_file_dir.empty())
-			{
-				new_part_file_dir = combine_path(new_save_path, m_part_file_dir);
-				create_directories(new_part_file_dir, e);
-				if (e) return;
-			}
-			else
-			{
-				new_part_file_dir = new_save_path;
-			}
-			m_part_file->move_partfile(new_part_file_dir, e);
+			m_part_file->move_partfile(new_save_path, m_part_file_dir, e);
 		};
 		std::tie(ret, m_save_path) = aux::move_storage(
 			names()
diff --git a/src/stat_cache.cpp b/src/stat_cache.cpp
index 5f20428fea267f51491f95e7094ada5fe8cd28b5..77a0fc241b243dc18f1ee0689868722639255ecc 100644
--- a/src/stat_cache.cpp
+++ b/src/stat_cache.cpp
@@ -81,8 +81,7 @@ namespace libtorrent::aux {
 		{
 			// query the filesystem
 			file_status s;
-			std::string const file_path = fn.file_path(i, save_path);
-			stat_file(file_path, &s, ec);
+			confined_stat_file(save_path, fn.file_path(i), &s, ec);
 			if (ec)
 			{
 				set_error_impl(i, ec);
diff --git a/src/storage_utils.cpp b/src/storage_utils.cpp
index 70f353acc0716e0cb1a91f9f34749c7667f58c08..2ba9d7765a1f96a1bbeeea8baa03c50bad680fbb 100644
--- a/src/storage_utils.cpp
+++ b/src/storage_utils.cpp
@@ -56,7 +56,7 @@ namespace libtorrent { namespace aux {
 					// files moved out to absolute paths are ignored
 					if (f.file_absolute_path(i)) continue;
 
-					stat_file(f.file_path(i, new_save_path), &s, err);
+					confined_stat_file(new_save_path, f.file_path(i), &s, err);
 					if (err != boost::system::errc::no_such_file_or_directory)
 					{
 						ec.ec = err;
@@ -110,11 +110,11 @@ namespace libtorrent { namespace aux {
 			// files moved out to absolute paths are not moved
 			if (f.file_absolute_path(i)) continue;
 
-			std::string const old_path = combine_path(save_path, f.file_path(i));
-			std::string const new_path = combine_path(new_save_path, f.file_path(i));
+			std::string const relative_path = f.file_path(i);
 
 			error_code ignore;
-			if (flags == move_flags_t::dont_replace && exists(new_path, ignore))
+			if (flags == move_flags_t::dont_replace
+				&& confined_exists(new_save_path, relative_path, ignore))
 			{
 				ret |= disk_status::need_full_check;
 				continue;
@@ -123,7 +123,8 @@ namespace libtorrent { namespace aux {
 			// TODO: ideally, if we end up copying files because of a move across
 			// volumes, the source should not be deleted until they've all been
 			// copied. That would let us rollback with higher confidence.
-			move_file(old_path, new_path, ec);
+			confined_move_file(save_path, relative_path
+				, new_save_path, relative_path, ec);
 
 			// if the source file doesn't exist. That's not a problem
 			// we just ignore that file
@@ -137,7 +138,8 @@ namespace libtorrent { namespace aux {
 				// on OSX, the error when trying to rename a file across different
 				// volumes is EXDEV, which will make it fall back to copying.
 				ec.ec.clear();
-				copy_file(old_path, new_path, ec);
+				confined_copy_file(save_path, relative_path
+					, new_save_path, relative_path, ec);
 				if (!ec) copied_files[i] = true;
 			}
 
@@ -173,12 +175,12 @@ namespace libtorrent { namespace aux {
 				// roll-back
 				if (copied_files[file_index]) continue;
 
-				std::string const old_path = combine_path(save_path, f.file_path(file_index));
-				std::string const new_path = combine_path(new_save_path, f.file_path(file_index));
+				std::string const relative_path = f.file_path(file_index);
 
 				// ignore errors when rolling back
 				storage_error ignore;
-				move_file(new_path, old_path, ignore);
+				confined_move_file(new_save_path, relative_path
+					, save_path, relative_path, ignore);
 			}
 
 			return { disk_status::fatal_disk_error, save_path };
@@ -203,23 +205,21 @@ namespace libtorrent { namespace aux {
 			// need to delete the source.
 			if (copied_files[i] == false) continue;
 
-			std::string const old_path = combine_path(save_path, f.file_path(i));
-
 			// we may still have some files in old save_path
 			// eg. if (flags == dont_replace && exists(new_path))
 			// ignore errors when removing
 			error_code ignore;
-			remove(old_path, ignore);
+			confined_remove(save_path, f.file_path(i), false, ignore);
 		}
 
 		for (std::string const& s : subdirs)
 		{
 			error_code err;
-			std::string subdir = combine_path(save_path, s);
+			std::string subdir = s;
 
-			while (!path_equal(subdir, save_path) && !err)
+			while (!subdir.empty() && !err)
 			{
-				remove(subdir, err);
+				confined_remove(save_path, subdir, true, err);
 				subdir = parent_path(subdir);
 			}
 		}
@@ -229,9 +229,10 @@ namespace libtorrent { namespace aux {
 
 	namespace {
 
-	void delete_one_file(std::string const& p, error_code& ec)
+	void delete_one_file(std::string const& root, std::string const& relative
+		, bool const directory, error_code& ec)
 	{
-		remove(p, ec);
+		confined_remove(root, relative, directory, ec);
 
 		if (ec == boost::system::errc::no_such_file_or_directory)
 			ec.clear();
@@ -250,20 +251,18 @@ namespace libtorrent { namespace aux {
 			for (auto const i : fs.file_range())
 			{
 				std::string const fp = fs.file_path(i);
-				bool const complete = fs.file_absolute_path(i);
-				std::string const p = complete ? fp : combine_path(save_path, fp);
-				if (!complete)
+				// Absolute renames are outside the storage root and are never owned
+				// by this confined storage operation.
+				if (fs.file_absolute_path(i)) continue;
+				std::string bp = parent_path(fp);
+				std::pair<iter_t, bool> ret;
+				ret.second = true;
+				while (ret.second && !bp.empty())
 				{
-					std::string bp = parent_path(fp);
-					std::pair<iter_t, bool> ret;
-					ret.second = true;
-					while (ret.second && !bp.empty())
-					{
-						ret = directories.insert(combine_path(save_path, bp));
-						bp = parent_path(bp);
-					}
+					ret = directories.insert(bp);
+					bp = parent_path(bp);
 				}
-				delete_one_file(p, ec.ec);
+				delete_one_file(save_path, fp, false, ec.ec);
 				if (ec) { ec.file(i); ec.operation = operation_t::file_remove; }
 			}
 
@@ -274,7 +273,7 @@ namespace libtorrent { namespace aux {
 				, end(directories.rend()); i != end; ++i)
 			{
 				error_code error;
-				delete_one_file(*i, error);
+				delete_one_file(save_path, *i, true, error);
 				if (error && !ec)
 				{
 					ec.file(file_index_t(-1));
@@ -289,7 +288,10 @@ namespace libtorrent { namespace aux {
 			|| (options & session::delete_files))
 		{
 			error_code error;
-			remove(part_file, error);
+			std::string const relative = lexically_relative_normal(
+				save_path, part_file, error);
+			if (!error)
+				delete_one_file(save_path, relative, false, error);
 			if (error && error != boost::system::errc::no_such_file_or_directory)
 			{
 				ec.file(file_index_t(-1));
@@ -659,4 +661,25 @@ std::int64_t get_filesize(stat_cache& stat, file_index_t const file_index
 			se.operation = operation_t::file_rename;
 	}
 
+	void confined_move_file(std::string const& old_root
+		, std::string const& old_relative, std::string const& new_root
+		, std::string const& new_relative, storage_error& se)
+	{
+		se.ec.clear();
+
+		if (has_parent_path(new_relative))
+		{
+			confined_create_directories(new_root, parent_path(new_relative), se.ec);
+			if (se)
+			{
+				se.operation = operation_t::mkdir;
+				return;
+			}
+		}
+
+		confined_rename(old_root, old_relative, new_root, new_relative, se.ec);
+		if (se)
+			se.operation = operation_t::file_rename;
+	}
+
 }}
diff --git a/test/test_file.cpp b/test/test_file.cpp
index 71a20be7fc7acdaf707148aa23bdcbbcc355333e..3732a764960c0108812f3bbdc4021c7b45123e0a 100644
--- a/test/test_file.cpp
+++ b/test/test_file.cpp
@@ -535,6 +535,161 @@ TORRENT_TEST(lexically_relative_normal)
 #undef S
 }
 
+#if defined __APPLE__ && defined __MACH__
+TORRENT_TEST(confined_filesystem_operations)
+{
+	std::string const root = complete("confined-file-root");
+	std::string const outside = complete("confined-file-outside");
+	error_code ec;
+	remove_all(root, ec);
+	ec.clear();
+	remove_all(outside, ec);
+	ec.clear();
+	create_directories(root, ec);
+	TEST_CHECK(!ec);
+	create_directories(outside, ec);
+	TEST_CHECK(!ec);
+	touch_file(combine_path(outside, "victim"), 32);
+
+	TEST_EQUAL(::symlink(outside.c_str(), combine_path(root, "escape").c_str()), 0);
+	TEST_EQUAL(::symlink(combine_path(outside, "victim").c_str()
+		, combine_path(root, "final-link").c_str()), 0);
+	TEST_EQUAL(::link(combine_path(outside, "victim").c_str()
+		, combine_path(root, "hard-link").c_str()), 0);
+
+	file_status status;
+	confined_stat_file(root, "escape/victim", &status, ec);
+	TEST_CHECK(ec);
+	ec.clear();
+	confined_stat_file(root, "../confined-file-outside/victim", &status, ec);
+	TEST_CHECK(ec);
+
+	bool open_failed = false;
+	try
+	{
+		aux::file_handle file(root, "escape/victim", 0
+			, aux::open_mode::write | aux::open_mode::truncate);
+	}
+	catch (storage_error const&)
+	{
+		open_failed = true;
+	}
+	TEST_CHECK(open_failed);
+
+	open_failed = false;
+	try
+	{
+		aux::file_handle file(root, "final-link", 0, aux::open_mode::read_only);
+	}
+	catch (storage_error const&)
+	{
+		open_failed = true;
+	}
+	TEST_CHECK(open_failed);
+
+	// A read-only open does not mutate the inode and remains valid. Writable
+	// opens must reject a multiply-linked inode so writes cannot escape root.
+	open_failed = false;
+	try
+	{
+		aux::file_handle file(root, "hard-link", 0, aux::open_mode::read_only);
+	}
+	catch (storage_error const&)
+	{
+		open_failed = true;
+	}
+	TEST_CHECK(!open_failed);
+
+	open_failed = false;
+	try
+	{
+		aux::file_handle file(root, "hard-link", 0
+			, aux::open_mode::write | aux::open_mode::truncate);
+	}
+	catch (storage_error const&)
+	{
+		open_failed = true;
+	}
+	TEST_CHECK(open_failed);
+
+	confined_remove(root, "escape/victim", false, ec);
+	TEST_CHECK(ec);
+	ec.clear();
+	stat_file(combine_path(outside, "victim"), &status, ec);
+	TEST_CHECK(!ec);
+	TEST_EQUAL(status.file_size, 32);
+
+	confined_create_directories(root, "safe/nested/", ec);
+	TEST_CHECK(!ec);
+	{
+		aux::file_handle file(root, "safe/nested/data", 16
+			, aux::open_mode::write | aux::open_mode::truncate);
+	}
+	confined_stat_file(root, "safe/nested/data", &status, ec);
+	TEST_CHECK(!ec);
+	TEST_EQUAL(status.file_size, 16);
+	confined_symlink(root, "nested/data", "safe/source-link", ec);
+	TEST_CHECK(!ec);
+	std::string link_target;
+	confined_read_symlink(root, "safe/source-link", link_target, ec);
+	TEST_CHECK(!ec);
+	TEST_EQUAL(link_target, "nested/data");
+	storage_error link_copy_error;
+	aux::confined_copy_file(root, "safe/source-link"
+		, root, "safe/copied-link", link_copy_error);
+	TEST_CHECK(!link_copy_error);
+	confined_read_symlink(root, "safe/copied-link", link_target, ec);
+	TEST_CHECK(!ec);
+	TEST_EQUAL(link_target, "nested/data");
+	confined_rename(root, "safe/source-link", root, "safe/moved-link", ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "safe/moved-link", false, ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "safe/copied-link", false, ec);
+	TEST_CHECK(!ec);
+
+	storage_error copy_error;
+	aux::confined_copy_file(root, "safe/nested/data"
+		, root, "escape/copied", copy_error);
+	TEST_CHECK(copy_error);
+	TEST_CHECK(!exists(combine_path(outside, "copied"), ec));
+	TEST_CHECK(!ec);
+	confined_rename(root, "safe/nested/data", root, "escape/moved", ec);
+	TEST_CHECK(ec);
+	ec.clear();
+	TEST_CHECK(confined_exists(root, "safe/nested/data", ec));
+	TEST_CHECK(!ec);
+	confined_create_directories(root, "escape/created/", ec);
+	TEST_CHECK(ec);
+	ec.clear();
+	TEST_CHECK(!exists(combine_path(outside, "created"), ec));
+	TEST_CHECK(!ec);
+
+	confined_rename(root, "safe/nested/data", root, "safe/nested/moved", ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "safe/nested/moved", false, ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "safe/nested/", true, ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "safe/", true, ec);
+	TEST_CHECK(!ec);
+
+	confined_remove(root, "escape", false, ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "final-link", false, ec);
+	TEST_CHECK(!ec);
+	confined_remove(root, "hard-link", false, ec);
+	TEST_CHECK(!ec);
+	stat_file(combine_path(outside, "victim"), &status, ec);
+	TEST_CHECK(!ec);
+	TEST_EQUAL(status.file_size, 32);
+	remove_all(root, ec);
+	TEST_CHECK(!ec);
+	remove_all(outside, ec);
+	TEST_CHECK(!ec);
+}
+#endif
+
 // UNC tests
 #if TORRENT_USE_UNC_PATHS
 
@@ -700,4 +855,3 @@ TORRENT_TEST(to_file_open_mode)
 	TEST_CHECK(aux::to_file_open_mode(aux::open_mode::write | aux::open_mode::no_atime, false) == (file_open_mode::read_write | file_open_mode::no_atime));
 	TEST_CHECK(aux::to_file_open_mode(aux::open_mode::write, true) == (file_open_mode::read_write | file_open_mode::mmapped));
 }
-
diff --git a/test/test_storage.cpp b/test/test_storage.cpp
index fac4bfff65e4339f12bb732ea19feac9ca983ca4..d976702b9a1dd6a6de4cd6bae6e251a117a5f69f 100644
--- a/test/test_storage.cpp
+++ b/test/test_storage.cpp
@@ -332,6 +332,79 @@ std::vector<char> new_piece(std::size_t const size)
 	return ret;
 }
 
+#if defined __APPLE__ && defined __MACH__
+template <typename StorageType>
+void test_confined_hard_link_write(char const* const name)
+{
+	std::string const root = complete(std::string("hard-link-") + name + "-root");
+	std::string const victim = complete(std::string("hard-link-") + name + "-victim");
+	error_code ec;
+	remove_all(root, ec);
+	ec.clear();
+	remove(victim, ec);
+	ec.clear();
+	create_directories(root, ec);
+	TEST_CHECK(!ec);
+	std::vector<char> metadata;
+	auto const info = setup_torrent_info(metadata);
+	std::string const linked = combine_path(root, info->layout().file_path(0_file));
+	create_directories(parent_path(linked), ec);
+	TEST_CHECK(!ec);
+
+	std::vector<char> const expected(0x8000, 'v');
+	{
+		std::ofstream output(victim, std::ios::binary);
+		output.write(expected.data(), std::streamsize(expected.size()));
+		TEST_CHECK(output.good());
+	}
+	TEST_EQUAL(::link(victim.c_str(), linked.c_str()), 0);
+
+	aux::vector<download_priority_t, file_index_t> priorities;
+	renamed_files renamed;
+	storage_params params{
+		info->layout(),
+		renamed,
+		root,
+		{},
+		storage_mode_sparse,
+		priorities,
+		sha1_hash{},
+		info->v1(),
+		info->v2()
+	};
+	typename file_pool_type<StorageType>::type pool;
+	auto storage = make_storage<StorageType>(params, pool);
+	aux::session_settings settings;
+	storage_error error;
+	storage->initialize(settings, error);
+	TEST_CHECK(!error);
+
+	std::vector<char> payload(0x4000, 'x');
+	int const written = write(storage, settings, span<char>(payload), 0_piece, 0
+		, aux::open_mode::write, error);
+	TEST_CHECK(error);
+	TEST_CHECK(written < int(payload.size()));
+
+	storage_error release_error;
+	release_files(storage, release_error);
+	TEST_CHECK(!release_error);
+	storage.reset();
+
+	std::vector<char> actual(expected.size());
+	{
+		std::ifstream input(victim, std::ios::binary);
+		input.read(actual.data(), std::streamsize(actual.size()));
+		TEST_EQUAL(input.gcount(), std::streamsize(actual.size()));
+	}
+	TEST_CHECK(actual == expected);
+
+	remove_all(root, ec);
+	TEST_CHECK(!ec);
+	remove(victim, ec);
+	TEST_CHECK(!ec);
+}
+#endif
+
 template <typename StorageType>
 void run_storage_tests(std::shared_ptr<torrent_info const> info
 	, lt::storage_mode_t storage_mode)
@@ -910,6 +983,20 @@ TORRENT_TEST_DISK_IO(check_files_allocate) { test_check_files_all_threads(zero_p
 TORRENT_TEST(test_pre_allocate_mmap) { test_pre_allocate<mmap_storage>(); }
 #endif
 
+#if defined __APPLE__ && defined __MACH__
+TORRENT_TEST(confined_hard_link_write_pread)
+{
+	test_confined_hard_link_write<pread_storage>("pread");
+}
+
+#if TORRENT_HAVE_MMAP || TORRENT_HAVE_MAP_VIEW_OF_FILE
+TORRENT_TEST(confined_hard_link_write_mmap)
+{
+	test_confined_hard_link_write<mmap_storage>("mmap");
+}
+#endif
+#endif
+
 // posix_storage is meant to only use the most portable API for disk I/O, and so
 // doesn't support pre-allocating files
 /*
