diff --git a/include/libtorrent/aux_/peer_list.hpp b/include/libtorrent/aux_/peer_list.hpp
index ca9f8bda1..5ec0a2fb1 100644
--- a/include/libtorrent/aux_/peer_list.hpp
+++ b/include/libtorrent/aux_/peer_list.hpp
@@ -19,7 +19,6 @@ see LICENSE file.
 #include <algorithm>
 
 #include "libtorrent/fwd.hpp"
-#include "libtorrent/aux_/string_util.hpp" // for allocate_string_copy
 #include "libtorrent/aux_/request_blocks.hpp" // for source_rank
 
 #include "libtorrent/aux_/torrent_peer.hpp"
diff --git a/include/libtorrent/aux_/pool.hpp b/include/libtorrent/aux_/pool.hpp
index 618378363..f70b43d88 100644
--- a/include/libtorrent/aux_/pool.hpp
+++ b/include/libtorrent/aux_/pool.hpp
@@ -15,6 +15,12 @@ see LICENSE file.
 #include "libtorrent/aux_/disable_warnings_pop.hpp"
 
 #include <cstddef>
+#include <cstdlib>
+#include <new>
+
+#if defined(__APPLE__)
+#include <malloc/malloc.h>
+#endif
 
 namespace libtorrent {
 namespace aux {
@@ -25,10 +31,43 @@ struct allocator_new_delete
 	using difference_type = std::ptrdiff_t;
 
 	// TODO: ensure the alignment is good here
+#if defined(_MALLOC_TYPE_ENABLED) && _MALLOC_TYPE_ENABLED
+	// A Boost pool allocation contains both its free-list links and raw object
+	// storage. Describe that backing allocation rather than the objects later
+	// constructed in its individual chunks.
+	struct pool_allocation
+	{
+		void* free_list;
+		std::max_align_t storage;
+	};
+
+	static void* allocate(size_type const bytes)
+		_MALLOC_TYPED(malloc_type_malloc, 1);
+
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wallocator-wrappers"
+	// Boost requires this exact wrapper signature. The inner annotated call
+	// preserves the pool-allocation descriptor despite the required char* API.
+	static char* malloc(size_type const bytes)
+	{
+		auto* const block = static_cast<pool_allocation*>(allocate(bytes));
+		if (block == nullptr) throw std::bad_alloc();
+		return reinterpret_cast<char*>(block);
+	}
+#pragma clang diagnostic pop
+#else
 	static char* malloc(size_type const bytes)
 	{ return new char[bytes]; }
+#endif
+
 	static void free(char* const block)
-	{ delete [] block; }
+	{
+#if defined(_MALLOC_TYPE_ENABLED) && _MALLOC_TYPE_ENABLED
+		std::free(block);
+#else
+		delete [] block;
+#endif
+	}
 };
 
 using pool = boost::pool<allocator_new_delete>;
diff --git a/include/libtorrent/aux_/string_util.hpp b/include/libtorrent/aux_/string_util.hpp
index a5729ea9b..937deeb92 100644
--- a/include/libtorrent/aux_/string_util.hpp
+++ b/include/libtorrent/aux_/string_util.hpp
@@ -107,11 +107,6 @@ namespace libtorrent::aux {
 	TORRENT_EXTRA_EXPORT void parse_comma_separated_string(
 		std::string const& in, std::vector<std::string>& out);
 
-	// strdup is not part of the C standard. Some systems
-	// don't have it and it won't be available when building
-	// in strict ANSI mode
-	TORRENT_EXTRA_EXPORT char* allocate_string_copy(string_view str);
-
 	// searches for separator ('sep') in the string 'last'.
 	// if found, returns the string_view representing the range from the start of
 	// `last` up to (but not including) the separator. The second return value is
diff --git a/include/libtorrent/aux_/tracker_list.hpp b/include/libtorrent/aux_/tracker_list.hpp
index da21215f5..abbd0b1fa 100644
--- a/include/libtorrent/aux_/tracker_list.hpp
+++ b/include/libtorrent/aux_/tracker_list.hpp
@@ -14,6 +14,7 @@ see LICENSE file.
 #include <unordered_map>
 
 #include "libtorrent/aux_/announce_entry.hpp"
+#include "libtorrent/aux_/pool.hpp"
 #include "libtorrent/announce_entry.hpp"
 #include "libtorrent/aux_/vector.hpp"
 #include "libtorrent/aux_/invariant_check.hpp"
@@ -88,7 +89,9 @@ private:
 	void check_invariant() const;
 #endif
 
-	boost::object_pool<aux::announce_entry> m_storage;
+	using tracker_pool = boost::object_pool<aux::announce_entry
+		, aux::allocator_new_delete>;
+	tracker_pool m_storage;
 	trackers_t m_trackers;
 	std::unordered_map<string_view, aux::announce_entry*> m_url_index;
 
@@ -98,4 +101,3 @@ private:
 
 }
 #endif
-
diff --git a/include/libtorrent/entry.hpp b/include/libtorrent/entry.hpp
index 6d78b6fb5..bb1ffb329 100644
--- a/include/libtorrent/entry.hpp
+++ b/include/libtorrent/entry.hpp
@@ -40,6 +40,7 @@ see LICENSE file.
 
 #include <map>
 #include <list>
+#include <memory>
 #include <string>
 #include <vector>
 #include <stdexcept>
@@ -71,7 +72,9 @@ namespace libtorrent {
 
 	namespace entry_types {
 
-		using dictionary_type = boost::container::map<std::string, entry, aux::strview_less>;
+		using dictionary_value_type = std::pair<std::string const, entry>;
+		using dictionary_type = boost::container::map<std::string, entry
+			, aux::strview_less, std::allocator<dictionary_value_type>>;
 		using string_type = std::string;
 		using list_type = std::vector<entry>;
 		using integer_type = std::int64_t;
diff --git a/src/bt_peer_connection.cpp b/src/bt_peer_connection.cpp
index 93dc0c367..dab91bab0 100644
--- a/src/bt_peer_connection.cpp
+++ b/src/bt_peer_connection.cpp
@@ -20,6 +20,7 @@ see LICENSE file.
 #include "libtorrent/config.hpp"
 
 #include <memory> // unique_ptr
+#include <new>
 #include <vector>
 #include <functional>
 
@@ -3063,7 +3064,13 @@ namespace {
 			{
 				TORRENT_ASSERT(m_sync_bytes_read == 0);
 
-				m_sync_vc.reset(new (std::nothrow) char[8]);
+				try
+				{
+					m_sync_vc.reset(new char[8]);
+				}
+				catch (std::bad_alloc const&)
+				{
+				}
 				if (!m_sync_vc)
 				{
 					disconnect(errors::no_memory, operation_t::encryption);
diff --git a/src/disk_buffer_pool.cpp b/src/disk_buffer_pool.cpp
index cfe9d76f0..fccf65f27 100644
--- a/src/disk_buffer_pool.cpp
+++ b/src/disk_buffer_pool.cpp
@@ -33,9 +33,29 @@ see LICENSE file.
 
 #include "libtorrent/aux_/disable_warnings_pop.hpp"
 
+#include <cstdlib>
+
+#if defined(__APPLE__)
+#include <malloc/malloc.h>
+#endif
+
 namespace libtorrent {
 namespace aux {
 
+#if defined(_MALLOC_TYPE_ENABLED) && _MALLOC_TYPE_ENABLED
+namespace {
+
+	struct disk_buffer_allocation
+	{
+		char storage[default_block_size];
+	};
+
+}
+
+	void* allocate_disk_buffer(std::size_t bytes)
+		_MALLOC_TYPED(malloc_type_malloc, 1);
+#endif
+
 	disk_buffer_pool::disk_buffer_pool() = default;
 
 	disk_buffer_pool::~disk_buffer_pool()
@@ -52,6 +72,13 @@ namespace aux {
 		return allocate_buffer_impl(l, category);
 	}
 
+#if defined(_MALLOC_TYPE_ENABLED) && _MALLOC_TYPE_ENABLED
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wallocator-wrappers"
+#endif
+	// Keep allocation inside the pool's accounting lock. This raw-pointer API
+	// cannot expose its fixed disk-block type to callers, so the inner annotated
+	// call supplies that descriptor directly.
 	char* disk_buffer_pool::allocate_buffer_impl(std::unique_lock<std::mutex>& l
 		, char const* category)
 	{
@@ -60,7 +87,13 @@ namespace aux {
 		TORRENT_UNUSED(l);
 		TORRENT_UNUSED(category);
 
+#if defined(_MALLOC_TYPE_ENABLED) && _MALLOC_TYPE_ENABLED
+		auto* const allocation = static_cast<disk_buffer_allocation*>(
+			allocate_disk_buffer(default_block_size));
+		char* ret = reinterpret_cast<char*>(allocation);
+#else
 		char* ret = static_cast<char*>(std::malloc(default_block_size));
+#endif
 
 		if (ret == nullptr)
 		{
@@ -87,6 +120,9 @@ namespace aux {
 
 		return ret;
 	}
+#if defined(_MALLOC_TYPE_ENABLED) && _MALLOC_TYPE_ENABLED
+#pragma clang diagnostic pop
+#endif
 
 	void disk_buffer_pool::free_multiple_buffers(span<char*> bufvec)
 	{
diff --git a/src/enum_net.cpp b/src/enum_net.cpp
index c91da7300..26283f64e 100644
--- a/src/enum_net.cpp
+++ b/src/enum_net.cpp
@@ -26,6 +26,7 @@ see LICENSE file.
 
 #include <functional>
 #include <cstdlib> // for wcstombscstombs
+#include <new>
 #include <optional>
 
 #include "libtorrent/aux_/disable_warnings_push.hpp"
@@ -1157,7 +1158,14 @@ int _System __libsocket_sysctl(int* mib, u_int namelen, void *oldp, size_t *oldl
 		return std::vector<ip_route>();
 	}
 
-	std::unique_ptr<char[]> buf(new (std::nothrow) char[needed]);
+	std::unique_ptr<char[]> buf;
+	try
+	{
+		buf.reset(new char[needed]);
+	}
+	catch (std::bad_alloc const&)
+	{
+	}
 	if (buf == nullptr)
 	{
 		ec = boost::asio::error::no_memory;
diff --git a/src/file_storage.cpp b/src/file_storage.cpp
index c20c45f45..e0c904ced 100644
--- a/src/file_storage.cpp
+++ b/src/file_storage.cpp
@@ -13,7 +13,7 @@ see LICENSE file.
 */
 
 #include "libtorrent/file_storage.hpp"
-#include "libtorrent/aux_/string_util.hpp" // for allocate_string_copy
+#include "libtorrent/aux_/string_util.hpp"
 #include "libtorrent/index_range.hpp"
 #include "libtorrent/aux_/path.hpp"
 #include "libtorrent/aux_/numeric_cast.hpp"
@@ -452,7 +452,10 @@ namespace aux {
 		}
 		else
 		{
-			name = aux::allocate_string_copy(n);
+			auto* const owned_name = new char[n.size() + 1];
+			std::copy(n.begin(), n.end(), owned_name);
+			owned_name[n.size()] = '\0';
+			name = owned_name;
 			name_len = name_is_owned;
 		}
 	}
diff --git a/src/ip_voter.cpp b/src/ip_voter.cpp
index fdfb06bdf..80b71e0cb 100644
--- a/src/ip_voter.cpp
+++ b/src/ip_voter.cpp
@@ -104,12 +104,22 @@ namespace libtorrent::aux {
 			{
 				if (random(1)) return maybe_rotate();
 
-				// use stable sort here to maintain the fifo-order
-				// of the entries with the same number of votes
-				// this will sort in ascending order, i.e. the lowest
-				// votes first. Also, the oldest are first, so this
-				// is a sort of weighted LRU.
-				std::stable_sort(m_external_addresses.begin(), m_external_addresses.end());
+				// Keep the FIFO order of entries with equal vote rank without
+				// std::stable_sort's untyped nothrow temporary allocation. This
+				// collection is bounded to 41 entries, so insertion sort is cheap.
+				for (auto current = m_external_addresses.begin() + 1
+					; current != m_external_addresses.end(); ++current)
+				{
+					auto value = std::move(*current);
+					auto position = current;
+					while (position != m_external_addresses.begin()
+						&& value < *(position - 1))
+					{
+						*position = std::move(*(position - 1));
+						--position;
+					}
+					*position = std::move(value);
+				}
 
 				// erase the last element, since it is one of the
 				// ones with the fewest votes
diff --git a/src/string_util.cpp b/src/string_util.cpp
index 51b30a80b..a51bc77d8 100644
--- a/src/string_util.cpp
+++ b/src/string_util.cpp
@@ -123,15 +123,6 @@ namespace libtorrent::aux {
 		return static_cast<int>(it - target.begin());
 	}
 
-	char* allocate_string_copy(string_view str)
-	{
-		if (str.empty()) return nullptr;
-		auto* tmp = new char[str.size() + 1];
-		std::copy(str.data(), str.data() + str.size(), tmp);
-		tmp[str.size()] = '\0';
-		return tmp;
-	}
-
 #if TORRENT_ABI_VERSION == 1 \
 	|| !defined TORRENT_DISABLE_LOGGING
 	std::string print_listen_interfaces(std::vector<listen_interface_t> const& in)
diff --git a/src/torrent.cpp b/src/torrent.cpp
index bde9188fb..3337e5edd 100644
--- a/src/torrent.cpp
+++ b/src/torrent.cpp
@@ -46,6 +46,7 @@ see LICENSE file.
 #include <memory>
 #include <numeric>
 #include <limits> // for numeric_limits
+#include <new>
 #include <cstdio> // for snprintf
 #include <functional>
 
@@ -852,7 +853,13 @@ aux::vector<download_priority_t, piece_index_t> file_to_piece_prio(
 		}
 
 		std::shared_ptr<read_piece_struct> rp = std::make_shared<read_piece_struct>();
-		rp->piece_data.reset(new (std::nothrow) char[std::size_t(piece_size)]);
+		try
+		{
+			rp->piece_data.reset(new char[std::size_t(piece_size)]);
+		}
+		catch (std::bad_alloc const&)
+		{
+		}
 		if (!rp->piece_data)
 		{
 			m_ses.alerts().emplace_alert<read_piece_alert>(
diff --git a/src/tracker_list.cpp b/src/tracker_list.cpp
index 303c1a257..863f54b0e 100644
--- a/src/tracker_list.cpp
+++ b/src/tracker_list.cpp
@@ -145,8 +145,8 @@ void tracker_list::replace(std::vector<lt::announce_entry> const& aes)
 	m_trackers.clear();
 	m_url_index.clear();
 
-	m_storage.~object_pool<aux::announce_entry>();
-	new (&m_storage) boost::object_pool<aux::announce_entry>();
+	m_storage.~tracker_pool();
+	new (&m_storage) tracker_pool();
 
 	for (auto const& ae : aes)
 	{
